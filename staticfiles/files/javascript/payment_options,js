// $(function() {
//   $('[data-toggle="tooltip"]').tooltip()
// })

// const callToActionBtns = document.getElementsByClassName(".nav-link");

// callToActionBtns.forEach((li) => {
//   li.addEventListener("click", (e) => {
//     callToActionBtns.forEach(f => f.classList.remove('active'));
//     li.target.classList.toggle("active");
//   });
// });

// Get the container element
var btnContainer = document.ClassName("nav");

// Get all buttons with class="btn" inside the container
var links = document.getElementsByClassName("nav-link");

// Loop through the buttons and add the active class to the current/clicked button
for (var i = 0; i < links.length; i++) {
  links[i].addEventListener("click", function() {
    var current = document.getElementsByClassName("nav-link active");

    // If there's no active class
    if (current.length > 0) {
      current[0].className = current[0].className.replace("nav-link active", "nav-link");
    }

    // Add the active class to the current/clicked button
    this.className += "nav-link active";
  });
}
